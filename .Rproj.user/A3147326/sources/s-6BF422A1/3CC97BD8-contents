# Facetas
  # Una forma de agregar variables adicionales es con la estética. Otra form,
  # particularmente útil para las variables catégoricas, es dividir su gráfico en facetas,
  # subgráficos que muestran cada uno un subconjunto de los datos

# Una variable
library(tidyverse)
ggplot(data = mpg) +
  geom_point(mapping = aes(x = displ, y = hwy)) +
  facet_wrap(~ class, nrow = 2)
# La variable que pase a face_wrap() debe ser discreta

# Dos variables
ggplot(data = mpg) +
  geom_point(mapping = aes(x = displ, y = hwy)) +
  facet_grid(drv ~ cyl)
# Si prefiere no facetar en la dimensión de filas o columnas,
# use un . en lugar de un nombre de variable, p. face_grid(. ~ cyl)

# 1) Se ocasiona un error  
ggplot(data = mpg) +
  geom_point(mapping = aes(x = displ, y = hwy)) +
  facet_wrap(~ disp, nrow = 2)

# 2)
